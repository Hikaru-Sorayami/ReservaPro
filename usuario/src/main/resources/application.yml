spring:
  application:
    name: usuario
  datasource:
    url: jdbc:mysql://localhost:3306/ms-usuario
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8081

springdoc:
  swagger-ui:
    path: /swagger-ui.html
