spring:
  application:
    name: pago
  datasource:
    url: jdbc:mysql://localhost:3306/ms-pago
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8083

springdoc:
  swagger-ui:
    path: /swagger-ui.html
