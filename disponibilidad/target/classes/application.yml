spring:
  application:
    name: disponibilidad
  datasource:
    url: jdbc:mysql://localhost:3306/ms-disponibilidad
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8085

springdoc:
  swagger-ui:
    path: /swagger-ui.html
